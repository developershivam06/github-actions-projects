[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "github-actions-calculator"
version = "1.0.0"
description = "Calculator package for GitHub Actions Project 2"
requires-python = ">=3.12"